{
  "name": "test-genie",
  "version": "0.0.0",
  "description": "Test Generator for Web Apps",
  "main": "src/index.js",
  "scripts": {
    "start": "ts-node-dev --respawn --no-notify --ignore-watch node_modules src/index.ts",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  },
  "dependencies": {
    "typescript": "^5.0.2"
  }
}
